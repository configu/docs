---
id: cli-overview
slug: cli-overview
title: CLI
---

import CliInstallSnippet from '../components/cli-install.mdx';

The Configu CLI is part of Configu's [open source software](https://github.com/configu/configu) and is one of the most popular interfaces among our users.
Like the other interfaces, the CLI is used for **managing** (structure, create, edit, delete, or export) configs and **orchestrating** the process by automating it, letting you sync with any storage you use for your configurations.

For a step-by-step introduction, you can get started with [this guide](/get-started).

## Installation

<CliInstallSnippet />

## Synopsis

```shell
configu <command> [--string-flag <value>] [--boolean-flag]
configu <topic> <command> [flags]
```

Use `configu <command> --help` for information on a specific command. Use `configu <topic> --help` to view a list of available help topics. The synopsis for each command shows its flags and their usage. Optional parameters are shown in square brackets.

## Command Reference

- [`configu init` - create a .cfgu file](/cli-init)
- [`configu login` - connect to Configu store](/cli-login)
- [`configu upsert` - manage Configs](/cli-upsert)
- [`configu eval` - fetch configuration data](/cli-eval)
- [`configu export` - format configuration data](/cli-export)
- [`configu delete` - delete Configs](/cli-delete)

See also:
- [Explore the Full CLI documentation](/cli-setup)