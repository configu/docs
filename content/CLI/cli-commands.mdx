---
id: cli-commands
slug: cli-commands
title: CLI Reference Documentation
---

This section contains reference documentation for working with the Configu CLI. For a step-by-step introduction, you can get started with [this guide](/get-started)..

## Synopsis

```shell
configu <command> [--string-flag <value>] [--boolean-flag]
configu <topic> <command> [flags]
```

Use `configu <command> --help` for information on a specific command. Use `configu <topic> --help` to view a list of available help topics. The synopsis for each command shows its flags and their usage. Optional parameters are shown in square brackets.

## Command Reference

- [`configu init` - create a .cfgu file](/cli-init)
- [`configu test` - create a .cfgu file](/cli-test)
- [`configu upsert` - create/update/delete Configs](/cli-upsert)
- [`configu delete` - bulk delete Configs](/cli-delete)
- [`configu eval` - fetch Configs](/cli-eval)
- [`configu export` - foramt/inject configuration data](/cli-export)

## Other Commands

### `configu login`

Initiate interactive login session to connect to Configu ConfigStore

### `configu update`

Update the configu CLI

`-a, --available` install a specific version.
`-i, --interactive` interactively select version to install.
`-v, --version <semver>` install a specific version.
`--force` force a re-download of the requested version.

### `configu version`

Display the Configu CLI version

`--verbose` show additional information about the CLI.
`--json` format output as json.

### `configu help`

Display help for Configu CLI

### `configu autocomplete`

Display autocomplete installation instructions
